export enum WorkflowProcess {
  PatientInTake = 1,
  Referral = 2,
  AddEmployee = 3,
  AddVendor = 4,
  AddPhysician = 5
}

export enum EditHelperActionType {
  delete = 0,
  add = 1,
  update = 2,
  reActivate = 3
}
